# Calculate the area
"""take length and width as input"""
length = float(input("enter length: "))
width = float(input("enter width: "))

area = length * width 
perimeter = 2*(length + width)

print(f"The area of rectangle = {area}cm^2")
print(f"Perimeter of rectangle = {perimeter}")
